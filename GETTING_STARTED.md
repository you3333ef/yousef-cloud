# 🚀 دليل البدء السريع - المنصة السورية للتطوير

مرحباً بك في المنصة السورية الشاملة للتطوير! هذا الدليل سيساعدك على تشغيل المشروع في دقائق.

## 📋 المتطلبات

تأكد من توفر المتطلبات التالية على جهازك:

- **Node.js** إصدار 18 أو أحدث
- **npm** أو **yarn** أو **pnpm**
- محرر نصوص (VS Code مُوصى به)

### التحقق من المتطلبات

```bash
node --version   # يجب أن يكون 18+
npm --version    # يجب أن يكون 9+
```

## ⚡ التشغيل السريع (3 دقائق)

### 1. تثبيت التبعيات

```bash
npm install
```

أو إذا كنت تفضل pnpm (أسرع):

```bash
pnpm install
```

### 2. إعداد ملف البيئة

```bash
cp .env.example .env
```

ثم قم بتحرير ملف `.env` وإضافة مفاتيح API الخاصة بك:

```env
VITE_CONVEX_URL=https://your-convex-url.convex.cloud
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key
VITE_OPENAI_API_KEY=sk-your-openai-key
```

### 3. تشغيل الخادم التطوير

```bash
npm run dev
```

### 4. افتح المتصفح

انتقل إلى: http://localhost:3000

**🎉 مبروك! المنصة تعمل الآن!**

---

## 📖 شرح مفصل

### الخطوة 1: فهم هيكل المشروع

```
syrian-dev-platform/
├── src/
│   ├── components/       # مكونات React
│   │   ├── ai/          # الذكاء الاصطناعي
│   │   ├── editor/      # محرر الكود
│   │   ├── infrastructure/  # البنية التحتية
│   │   ├── packages/    # إدارة الحزم
│   │   └── ui/          # مكونات الهوية السورية
│   ├── lib/             # مكتبات مساعدة
│   ├── styles/          # ملفات CSS
│   ├── App.tsx          # التطبيق الرئيسي
│   └── main.tsx         # نقطة الدخول
├── public/              # ملفات عامة
├── docs/                # التوثيق
├── .env.example         # نموذج ملف البيئة
└── README.md            # دليل شامل
```

### الخطوة 2: إعداد مفاتيح API

#### OpenAI (للذكاء الاصطناعي)
1. اذهب إلى [platform.openai.com](https://platform.openai.com)
2. أنشئ حساباً أو سجل دخول
3. انتقل إلى API Keys
4. أنشئ مفتاح جديد
5. انسخ المفتاح إلى `.env`

#### Anthropic Claude
1. اذهب إلى [console.anthropic.com](https://console.anthropic.com)
2. أنشئ مفتاح API
3. أضفه إلى `.env`

#### Convex (قاعدة البيانات)
1. اذهب إلى [convex.dev](https://convex.dev)
2. أنشئ مشروعاً جديداً
3. انسخ URL من إعدادات المشروع
4. أضفه إلى `.env`

#### Supabase (المصادقة والتخزين)
1. اذهب إلى [supabase.com](https://supabase.com)
2. أنشئ مشروعاً جديداً
3. انتقل إلى Settings > API
4. انسخ URL و anon key
5. أضفهما إلى `.env`

### الخطوة 3: استكشاف الواجهة

بعد التشغيل، ستجد في الشريط الجانبي:

#### 1. 📝 محرر الكود
- اكتب الكود بلغات مختلفة
- احفظ أو ارفع ملفات
- تخصيص المظهر

#### 2. 🤖 الذكاء الاصطناعي
- اختر مزود AI من القائمة
- ابدأ محادثة
- انسخ الردود

#### 3. ⚙️ البنية التحتية
- اختر منصة النشر
- حدد قاعدة البيانات
- انشر التطبيق

#### 4. 📦 إدارة الحزم
- اختر مدير الحزم
- ابحث عن الحزم
- ثبت أو احذف الحزم

#### 5. ℹ️ حول المشروع
- تعلم عن الهوية السورية
- شاهد المعالم التاريخية
- اقرأ التوثيق

---

## 🎨 الهوية السورية

### الألوان الأساسية
- **أخضر سوري**: #054239
- **ذهبي قاسيون**: #b9a779
- **رملية تدمر**: #edebe0

### الرمز الوطني
العقاب الذهبي يرمز إلى:
- 14 ريشة جناح → المحافظات السورية
- 5 ريشات ذيل → المناطق الجغرافية
- 3 نجوم → الحرية والكرامة والاستمرارية

---

## 🔧 الأوامر المفيدة

### التطوير
```bash
npm run dev          # تشغيل خادم التطوير
npm run build        # بناء للإنتاج
npm run preview      # معاينة البناء
npm run lint         # فحص الكود
npm run type-check   # فحص الأنواع
```

### إدارة الحزم
```bash
npm install <pkg>    # تثبيت حزمة
npm uninstall <pkg>  # حذف حزمة
npm update          # تحديث الحزم
npm audit           # فحص الأمان
```

---

## 🐛 حل المشاكل الشائعة

### مشكلة: خطأ في puerto 3000
```bash
Error: Port 3000 is already in use
```
**الحل**: غيّر المنفذ:
```bash
npm run dev -- --port 3001
```

### مشكلة: خطأ في TypeScript
```bash
Type 'string | undefined' is not assignable...
```
**الحل**: تأكد من إعداد جميع متغيرات البيئة في `.env`

### مشكلة: بطء في التثبيت
**الحل**: استخدم pnpm بدلاً من npm:
```bash
npm install -g pnpm
pnpm install
```

### مشكلة: خطأ في @monaco-editor/react
**الحل**: تأكد من استخدام React 18+:
```bash
npm install react@latest react-dom@latest
```

---

## 🚀 النشر

### Vercel (سهل وسريع)
```bash
npm i -g vercel
vercel
```

### Netlify
```bash
npm run build
# ارفع مجلد dist/ إلى Netlify
```

### GitHub Pages
```bash
npm install -g gh-pages
npm run build
gh-pages -d dist
```

---

## 📚 موارد إضافية

- **[README.md](./README.md)** - دليل شامل
- **[DESIGN_SYSTEM.md](./DESIGN_SYSTEM.md)** - نظام التصميم
- **[React Docs](https://react.dev)** - وثائق React
- **[Tailwind CSS](https://tailwindcss.com)** - وثائق Tailwind
- **[Vite Guide](https://vitejs.dev/guide)** - دليل Vite

---

## 🤝 الحصول على المساعدة

إذا واجهت مشكلة:

1. **راجع التوثيق** في مجلد `docs/`
2. **ابحث في GitHub Issues**
3. **انضم لمجتمع المطورين**
4. **تواصل معنا عبر البريد**

---

## ❤️ رسالة من الفريق

هذه المنصة مصممة بحب وعناية لتكون home للمطورين السوريين. نأمل أن تساعدك في رحلتك البرمجية.

**مع حب ❤️ لسوريا**

---

**© 2025 المنصة السورية للتطوير**
